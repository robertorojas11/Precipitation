# Data Pipeline Configuration

# Training Hyperparameters
batch_size: 16
prefetch_buffer: 2 # tf.data.AUTOTUNE roughly equivalent

# Class Imbalance Configuration
w_wet: 5.0 # Weight assigned to patches containing significant rain
wet_threshold_mm: 1.0 # Minimum precipitation to consider a pixel "wet"

# Performance
num_parallel_calls: 4 # Number of parallel threads for dataset mapping

# Stakeholder / Placeholder: dataset_index.csv
# The pipeline relies on dataset_index.csv which is currently NOT being generated by the download script.
# TODO: Implement generate_dataset_index.py to scan the LOCAL_DATA_DIR and create this file.
